<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Taker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/note-taker.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>

<body data-user-id="<%= user ? user.id : '' %>">

    <main class="container-fluid">
        <div class="row" style="height: 100vh;">

            <!-- Sidebar (Notes List) -->
            <div class="col-md-4 col-12 p-3 bg-light border-end" style="overflow-y: auto;">
                <h3>Notes</h3>
                <div class="input-group mb-3">
                    <input type="text" id="searchInput" class="form-control" placeholder="Search Notes">
                    <button class="btn btn-primary" type="button" id="searchBtn">
                        <i class="bi bi-search"></i>
                    </button>
                </div>
                <div id="noteList">
                    <!-- Notes will be inserted here dynamically -->
                </div>
            </div>

            <!-- Note Content -->
            <div class="col-md-8 col-12 p-4" id="noteArea">
                <!-- Tool bar -->
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div>
                        <!-- delete button -->
                        <button id="toolbarDeleteBtn" class="btn btn-outline-dark me-2"><i
                                class="bi bi-trash"></i></button>
                        <!-- edit button -->
                        <button id="toolbarCreateBtn" class="btn btn-outline-dark"><i class="bi bi-pencil"></i></button>
                    </div>
                    <!-- note title -->
                    <input type="text" id="NoteTitle" placeholder="Note Title">
                    <button class="btn btn-outline-dark"><i class="bi bi-upload"></i> share</button>
                </div>
                <!-- note content -->
                <textarea id="NoteContent" placeholder="Type your notes"></textarea>
                <button type="submit">Save</button>
            </div>

        </div>
    </main>

    <script src="/js/note-taker.js" defer></script>
</body>

</html>